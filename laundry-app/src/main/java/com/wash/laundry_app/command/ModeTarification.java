package com.wash.laundry_app.command;

public enum ModeTarification {
    PER_M2,
    PER_UNIT,
    PER_KG,
    PER_LINEAR_M,
    CUSTOM,
    SIZE_BASED, // Keep for backward compatibility if needed
    MANUAL      // Keep for backward compatibility if needed
}
